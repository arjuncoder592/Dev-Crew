import { currentStudent } from "@/lib/current-student"; import { prisma } from "@/lib/prisma"; import { redirect,notFound } from "next/navigation"; import AssessmentForm from "./form";
export default async function AssessmentPage({params}:{params:Promise<{id:string}>}){const student=await currentStudent();if(!student)redirect("/login");const a=await prisma.assessment.findUnique({where:{id:(await params).id},include:{skill:true,questions:true}});if(!a)notFound();const own=student.skills.find(s=>s.skillId===a.skillId)?.level??0;if(a.level>1&&own<a.level-1)redirect("/assessments");return <AssessmentForm assessment={{id:a.id,title:a.title,skill:a.skill.name,questions:a.questions.map(q=>({id:q.id,prompt:q.prompt,options:q.options as string[]}))}}/>}
