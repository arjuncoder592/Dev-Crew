import { Sidebar } from "@/components/sidebar"; import { currentStudent } from "@/lib/current-student"; import { redirect } from "next/navigation";
export default async function Skills(){const student=await currentStudent();if(!student)redirect("/login");return <div className="layout"><Sidebar/><main className="content"><h1>Your skill profile</h1><p className="muted">Self-declared is a claim. Assessed levels come from passed SkillBridge assessments; practical evidence remains independently reviewable.</p><div className="card"><table><thead><tr><th>Skill</th><th>Current standing</th><th>Next action</th></tr></thead><tbody>{student.skills.map(s=><tr key={s.id}><td><b>{s.skill.name}</b></td><td><span className={`tag ${s.level>0?"good":""}`}>{s.level?`Level ${s.level} — ${s.status.toLowerCase().replace("_"," ")}`:"Self-declared"}</span></td><td>{s.level<3?`Validate Level ${s.level+1}`:"Revalidate when needed"}</td></tr>)}</tbody></table></div></main></div>}
